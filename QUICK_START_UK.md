# 🚀 Швидкий старт - Система блоків

## 📦 Що вже створено

✅ **Система типів** - `src/types/blockTypes.ts` (60+ типів блоків)
✅ **Реєстр блоків** - `src/lib/blockRegistry.ts` (управління блоками)
✅ **Компонент вибору** - `src/components/BlockPicker.tsx`
✅ **Новий конфігуратор** - `src/pages/NewConfigurator.tsx`
✅ **Документація** - `BLOCK_SYSTEM_README.md` (повна документація)

## 🎯 Як запустити

### 1. Встановіть залежності (якщо ще не зробили)

```bash
cd /home/user/webapp
pnpm install
```

### 2. Запустіть dev сервер

```bash
pnpm dev
```

### 3. Відкрийте браузер

Перейдіть на `http://localhost:3000/builder`

## 🎨 Використання

### Додавання блоку

1. Натисніть кнопку **"+"** або **"Додати блок"**
2. Відкриється BlockPicker з усіма доступними блоками
3. Використайте:
   - **Вкладки** для фільтрації по категоріях
   - **Пошук** для швидкого знаходження
4. Клікніть на блок для додавання

### Редагування блоку

1. Виберіть блок зі списку зліва
2. В центрі відкриється редактор
3. Змініть:
   - Назву блоку
   - JSON конфігурацію (поки що)
   - Скоро: візуальні контроли

### Глобальні налаштування

1. Перейдіть на вкладку **"Глобальні"** справа
2. Налаштуйте:
   - Палітру кольорів
   - Типографіку
   - Відступи (скоро)

### Експорт

- **Копіювати** - скопіює JSON в буфер обміну
- **Експорт** - завантажить JSON файл

## 📂 Структура проекту

```
src/
├── types/
│   └── blockTypes.ts          # ВСІ типи блоків (60+)
│
├── lib/
│   └── blockRegistry.ts        # Реєстр та утиліти
│
├── components/
│   └── BlockPicker.tsx         # Діалог вибору блоків
│
├── pages/
│   ├── NewConfigurator.tsx     # Новий конфігуратор
│   └── Configurator.tsx        # Старий (backup на /builder-old)
│
└── client/src/App.tsx          # Роути
```

## 🔧 Доступні категорії блоків

1. **📝 Текст** (5 блоків) - Заголовки, параграфи, цитати, списки
2. **🖼️ Медіа** (6 блоків) - Зображення, галереї, відео, аудіо
3. **📦 Макет** (6 блоків) - Контейнери, колонки, акордеон, вкладки
4. **☰ Навігація** (5 блоків) - Меню, breadcrumbs, пагінація
5. **🔘 Кнопки та CTA** (4 блоки) - Кнопки, групи, FAB
6. **📝 Форми** (4 блоки) - Контактна, підписка, пошук, квіз
7. **🛒 E-Commerce** (5 блоків) - Товари, ціни, таймери
8. **📊 Дані та статистика** (3 блоки) - Графіки, лічильники, прогрес
9. **👥 Інші** - Соціальні, команда, події, блог тощо

## 🎯 Наступні кроки (TODO)

### В розробці:

- [ ] **Візуальні редактори** для кожного типу блоку
- [ ] **Drag & Drop** для зміни порядку блоків
- [ ] **Live Preview** з реальним рендерингом
- [ ] **Responsive контроли** (mobile/tablet/desktop перемикач)
- [ ] **Панелі налаштувань** для:
  - Відступів (padding/margin)
  - Рамок (border, radius)
  - Тіней (box-shadow)
  - Анімацій (entrance, hover effects)
- [ ] **Генератор HTML/CSS** на бекенді
- [ ] **Система шаблонів** (готові пресети)

## 💡 Швидкі приклади коду

### Створити блок програмно

```typescript
import { createBlockInstance } from '@/lib/blockRegistry';

const heading = createBlockInstance('heading', {
  name: 'Мій заголовок',
  config: {
    content: 'Привіт, світ!',
    tag: 'h1'
  }
});
```

### Знайти блоки

```typescript
import { searchBlocks, getBlocksByCategory } from '@/lib/blockRegistry';
import { BlockCategory } from '@/types/blockTypes';

// Пошук
const found = searchBlocks('кнопка');

// По категорії
const textBlocks = getBlocksByCategory(BlockCategory.TEXT);
```

### Отримати визначення блоку

```typescript
import { getBlockDefinition } from '@/lib/blockRegistry';

const def = getBlockDefinition('heading');
console.log(def?.nameUk); // "Заголовок"
console.log(def?.icon);   // "📝"
```

## 📚 Повна документація

Детальна документація в файлі **`BLOCK_SYSTEM_README.md`**

Там ви знайдете:
- Повний список всіх блоків
- Детальні приклади використання
- Інструкції з додавання нових блоків
- API довідка
- Troubleshooting

## 🐛 Проблеми?

1. **Блоки не відображаються**
   - Перевірте console на помилки
   - Переконайтеся, що імпорти правильні

2. **TypeScript помилки**
   - Запустіть `pnpm check`
   - Перевірте шляхи імпортів

3. **Компоненти не знайдено**
   - Перевірте наявність всіх UI компонентів в `src/components/ui/`

## 📞 Що далі?

### Швидкі дії:

1. **Відкрийте `/builder`** - подивіться на новий інтерфейс
2. **Додайте кілька блоків** - протестуйте BlockPicker
3. **Експортуйте JSON** - подивіться структуру даних
4. **Прочитайте BLOCK_SYSTEM_README.md** - повна документація

### Для розробки:

1. **Створіть редактор для конкретного блоку** (наприклад, HeadingEditor.tsx)
2. **Додайте генератор HTML/CSS** на бекенді
3. **Інтегруйте з існуючою БД** (якщо потрібно)
4. **Додайте шаблони** для швидкого старту

## 🎉 Готово!

Тепер у вас є потужна база для створення будь-яких блоків!

**Приємної роботи! 🚀**

---

## 📝 Примітки

- Старий конфігуратор доступний на `/builder-old`
- Новий конфігуратор на `/builder`
- Всі типи блоків вже створені та задокументовані
- Система спроектована для легкого розширення

## 🤝 Потрібна допомога?

Всі типи та функції мають JSDoc коментарі. Використовуйте автодоповнення вашого IDE!
