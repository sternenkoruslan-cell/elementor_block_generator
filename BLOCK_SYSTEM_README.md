# 🎨 Повна Система Блоків для WordPress/Elementor

## 📋 Зміст

1. [Огляд системи](#огляд-системи)
2. [Архітектура](#архітектура)
3. [Доступні блоки](#доступні-блоки)
4. [Як використовувати](#як-використовувати)
5. [Додавання нових блоків](#додавання-нових-блоків)
6. [API документація](#api-документація)

---

## 🎯 Огляд системи

Це повноцінна система конструктора блоків для створення готових HTML/CSS компонентів, які можна експортувати в Elementor або інші конструктори сайтів.

### Ключові особливості:

✅ **60+ типів блоків** - від простих заголовків до складних форм та e-commerce компонентів
✅ **16 категорій** - логічна організація всіх блоків
✅ **Повна типізація** - TypeScript для надійності та автодоповнення
✅ **Responsive дизайн** - налаштування для mobile, tablet, desktop
✅ **Візуальний редактор** - зручний інтерфейс для налаштування
✅ **Live Preview** - миттєвий попередній перегляд змін
✅ **Глобальні налаштування** - палітра кольорів, типографіка, відступи
✅ **Експорт** - готовий HTML/CSS код для вставки

---

## 🏗️ Архітектура

### Структура файлів:

```
src/
├── types/
│   └── blockTypes.ts          # Всі типи блоків та інтерфейси
├── lib/
│   └── blockRegistry.ts        # Реєстр блоків та утиліти
├── components/
│   ├── BlockPicker.tsx         # Компонент вибору блоків
│   ├── BlockEditor.tsx         # Редактор окремого блоку
│   ├── BlockPreview.tsx        # Превью блоку
│   └── GlobalSettings.tsx      # Глобальні налаштування
└── pages/
    └── Configurator.tsx        # Головна сторінка конструктора
```

### Основні класи та інтерфейси:

1. **BlockConfigUnion** - union type всіх можливих конфігурацій блоків
2. **BaseBlockConfig** - базова конфігурація з загальними властивостями
3. **BlockDefinition** - метадані блоку (назва, категорія, іконка тощо)
4. **GlobalSettings** - глобальні налаштування теми

---

## 📦 Доступні блоки

### 🎯 КОНТЕНТНІ БЛОКИ

#### 📝 Текстові елементи
- **Heading** (заголовок) - H1-H6 з налаштуваннями
- **Paragraph** (параграф) - текст з форматуванням
- **Quote** (цитата) - блочні, вбудовані, з автором
- **List** (список) - маркіровані, нумеровані, з іконками
- **Icon Text** (іконка+текст) - іконка з заголовком та описом

#### 🖼️ Медіа-блоки
- **Image** (зображення) - з підписом, лайтбоксом, фільтрами
- **Gallery** (галерея) - сітка, масонрі, слайдер, карусель
- **Video** (відео) - YouTube, Vimeo, власний хостинг
- **Audio Player** (аудіо) - з плейлистом
- **Interactive Image** (інтерактивне зображення) - з точками-підказками
- **Image Comparison** (порівняння) - до/після слайдер

### 📊 СТРУКТУРНІ БЛОКИ

- **Container** (контейнер) - повна ширина, boxed, fluid
- **Columns** (колонки) - гнучка сітка 1-12 колонок
- **Accordion** (акордеон) - згортаємі секції
- **Tabs** (вкладки) - горизонтальні, вертикальні
- **Modal** (модальне вікно) - popup, lightbox
- **Card** (картка) - картковий макет

### 🎨 ІНТЕРАКТИВНІ ЕЛЕМЕНТИ

#### Навігація
- **Menu** (меню) - горизонтальне, вертикальне, мега-меню
- **Breadcrumbs** (хлібні крихти)
- **Pagination** (пагінація)
- **Anchor Links** (якірні посилання)
- **Table of Contents** (зміст) - автогенерований

#### Кнопки та CTA
- **Button** (кнопка) - звичайні, 3D, анімовані
- **Button Group** (група кнопок)
- **FAB** (плаваюча кнопка)
- **CTA Section** (заклик до дії) - з фоном, анімацією

### 📝 ФОРМИ

- **Contact Form** (контактна форма) - багатокрокова опція
- **Newsletter Form** (форма підписки)
- **Search Form** (форма пошуку) - з live search
- **Quiz Form** (квіз) - багатокроковий з оцінкою

### 🛒 E-COMMERCE

- **Product Grid** (сітка товарів) - grid, list view
- **Product Card** (картка товару)
- **Add to Cart Button** (кнопка в кошик)
- **Pricing Table** (таблиця цін) - порівняння планів
- **Countdown Timer** (таймер) - зворотний відлік

### 📈 ДАНІ ТА СТАТИСТИКА

- **Progress Bar** (прогрес-бар) - горизонтальний, вертикальний, круговий
- **Counter** (лічильник) - анімовані числа
- **Chart** (графік) - pie, bar, line charts
- **Timeline** (таймлайн) - вертикальний, горизонтальний
- **Comparison Table** (таблиця порівняння)

### 👥 СОЦІАЛЬНІ ТА КОМУНІКАЦІЯ

- **Social Share** (шерити) - кнопки соцмереж
- **Testimonial** (відгуки) - слайдер, сітка
- **Comments** (коментарі)
- **FAQ** (питання-відповіді) - accordion style

### 🏢 БІЗНЕС-БЛОКИ

- **Team Member** (член команди) - картки з фото та контактами
- **Contact Info** (контактна інформація)
- **Map** (карта) - Google Maps, OpenStreetMap
- **Logo Carousel** (карусель логотипів)

### 📅 ПОДІЇ

- **Calendar** (календар) - подій
- **Event Card** (картка події)
- **Schedule** (розклад) - табличний розклад

### 📰 БЛОГ

- **Blog Grid** (сітка постів) - grid, list, masonry
- **Post Card** (картка посту)
- **Author Box** (блок автора)

### 🔧 СПЕЦІАЛЬНІ

- **Code Block** (блок коду) - з підсвічуванням
- **Embed** (вбудовування) - iframe, HTML
- **Alert** (сповіщення) - info, success, warning, error
- **Particles** (частинки) - фонові ефекти
- **Sticky Element** (липкий елемент)

---

## 🚀 Як використовувати

### 1. Базове використання

```typescript
import { createBlockInstance } from '@/lib/blockRegistry';
import { BLOCK_REGISTRY } from '@/lib/blockRegistry';

// Створити новий блок
const headingBlock = createBlockInstance('heading', {
  config: {
    content: 'Мій заголовок',
    tag: 'h1'
  }
});

// Отримати інформацію про блок
const blockDef = BLOCK_REGISTRY['heading'];
console.log(blockDef.nameUk); // "Заголовок"
```

### 2. Пошук блоків

```typescript
import { searchBlocks, getBlocksByCategory } from '@/lib/blockRegistry';
import { BlockCategory } from '@/types/blockTypes';

// Пошук за ключовим словом
const results = searchBlocks('кнопка');

// Отримати всі блоки категорії
const textBlocks = getBlocksByCategory(BlockCategory.TEXT);
```

### 3. Використання BlockPicker

```tsx
import { BlockPicker } from '@/components/BlockPicker';

function MyComponent() {
  const [pickerOpen, setPickerOpen] = useState(false);
  
  const handleBlockSelect = (blockType: string) => {
    const newBlock = createBlockInstance(blockType);
    // Додати блок до вашого стейту
  };
  
  return (
    <BlockPicker
      open={pickerOpen}
      onOpenChange={setPickerOpen}
      onSelectBlock={handleBlockSelect}
    />
  );
}
```

### 4. Налаштування блоку

Кожен блок має:
- **Базові налаштування** - відступи, рамки, тіні, фон
- **Responsive налаштування** - для mobile, tablet, desktop
- **Анімації** - вхід, вихід, при наведенні
- **Специфічні налаштування** - унікальні для типу блоку

```typescript
const block: BaseBlockConfig = {
  id: 'block_123',
  type: 'button',
  name: 'Моя кнопка',
  category: BlockCategory.BUTTONS_CTA,
  
  // Відступи
  spacing: {
    padding: {
      desktop: { all: '16px' },
      mobile: { all: '12px' }
    },
    margin: {
      desktop: { all: '20px' }
    }
  },
  
  // Рамка
  border: {
    width: '2px',
    style: 'solid',
    color: '#3b82f6',
    radius: { all: '8px' }
  },
  
  // Анімація
  animation: {
    type: 'fadeInUp',
    duration: 500,
    trigger: 'on-scroll'
  },
  
  // Специфічна конфігурація
  config: {
    type: 'button',
    text: 'Натисни мене',
    size: 'large',
    style: 'solid'
  }
};
```

---

## ➕ Додавання нових блоків

### Крок 1: Створити інтерфейс

Додайте в `src/types/blockTypes.ts`:

```typescript
export interface MyCustomBlockConfig {
  type: 'my-custom-block';
  customProperty?: string;
  anotherProperty?: number;
  // ... інші властивості
}

// Додати до union type
export type BlockConfigUnion = 
  | HeadingBlockConfig
  | MyCustomBlockConfig  // <-- додати тут
  | ... // інші типи
```

### Крок 2: Зареєструвати блок

Додайте в `src/lib/blockRegistry.ts`:

```typescript
export const BLOCK_REGISTRY: Record<string, BlockDefinition> = {
  // ... існуючі блоки
  
  'my-custom-block': {
    type: 'my-custom-block',
    name: 'My Custom Block',
    nameUk: 'Мій власний блок',
    category: BlockCategory.SPECIAL,
    description: 'Custom block description',
    descriptionUk: 'Опис мого блоку',
    icon: '🎨',
    keywords: ['custom', 'special'],
    defaultConfig: {
      type: 'my-custom-block',
      customProperty: 'default value',
      anotherProperty: 42
    }
  }
};
```

### Крок 3: Створити компонент редактора (опційно)

```tsx
// src/components/editors/MyCustomBlockEditor.tsx
export function MyCustomBlockEditor({ config, onChange }) {
  return (
    <div>
      <Input 
        value={config.customProperty}
        onChange={(e) => onChange({ ...config, customProperty: e.target.value })}
      />
      {/* ... інші поля */}
    </div>
  );
}
```

### Крок 4: Додати генератор HTML/CSS

В `server/routers.ts` додайте функцію генерації:

```typescript
function generateMyCustomBlockHTML(blockId: string, config: MyCustomBlockConfig): string {
  return `
    <div class="${blockId} my-custom-block">
      <p>${config.customProperty}</p>
      <!-- HTML структура блоку -->
    </div>
  `;
}

// Додати в switch statement функції generateBlockCode
case 'my-custom-block':
  html = generateMyCustomBlockHTML(blockId, config as MyCustomBlockConfig);
  break;
```

---

## 📚 API документація

### BlockRegistry API

#### `getBlocksByCategory(category: BlockCategory): BlockDefinition[]`
Повертає всі блоки певної категорії

#### `getCategoriesWithCounts(): Array<{ category, count, nameUk }>`
Повертає список категорій з кількістю блоків

#### `searchBlocks(query: string): BlockDefinition[]`
Пошук блоків за ключовим словом

#### `getBlockDefinition(type: string): BlockDefinition | undefined`
Отримати визначення блоку за типом

#### `createBlockInstance(type: string, overrides?): BaseBlockConfig`
Створити новий екземпляр блоку з дефолтними значеннями

### Типи анімацій

```typescript
type AnimationType = 
  | 'fadeIn' | 'fadeInUp' | 'fadeInDown'
  | 'slideIn' | 'slideInUp' | 'slideInDown'
  | 'bounce' | 'zoomIn' | 'rotateIn'
  | 'parallax' | 'kenBurns'
  // ... та інші
```

### Типи hover ефектів

```typescript
type HoverEffect = 
  | 'grow' | 'shrink' | 'float' | 'lift'
  | 'glow' | 'shadow' | 'brighten'
  | 'rotate' | 'skew'
  // ... та інші
```

### Responsive значення

```typescript
type ResponsiveValue<T> = {
  mobile?: T;
  tablet?: T;
  desktop?: T;
};

// Приклад
const fontSize: ResponsiveValue<string> = {
  mobile: '14px',
  tablet: '16px',
  desktop: '18px'
};
```

---

## 🎨 Глобальні налаштування

### Палітра кольорів

```typescript
const globalSettings: GlobalSettings = {
  colors: {
    primary: '#3b82f6',
    secondary: '#8b5cf6',
    accent: '#10b981',
    text: '#1f2937',
    textLight: '#6b7280',
    background: '#ffffff',
    backgroundAlt: '#f9fafb',
    border: '#e5e7eb',
    success: '#10b981',
    warning: '#f59e0b',
    error: '#ef4444',
    info: '#3b82f6'
  }
};
```

### Типографіка

```typescript
const globalSettings: GlobalSettings = {
  typography: {
    fontFamily: {
      primary: 'Inter, sans-serif',
      secondary: 'Roboto, sans-serif',
      headings: 'Poppins, sans-serif'
    },
    fontSize: {
      base: '16px',
      h1: '2.5rem',
      h2: '2rem',
      h3: '1.75rem',
      h4: '1.5rem',
      h5: '1.25rem',
      h6: '1rem'
    },
    fontWeight: {
      light: 300,
      normal: 400,
      medium: 500,
      semibold: 600,
      bold: 700
    }
  }
};
```

### Відступи та розміри

```typescript
const globalSettings: GlobalSettings = {
  spacing: {
    xs: '4px',
    sm: '8px',
    md: '16px',
    lg: '24px',
    xl: '32px',
    xxl: '48px'
  },
  borderRadius: {
    sm: '4px',
    md: '8px',
    lg: '12px',
    xl: '16px',
    full: '9999px'
  },
  shadows: {
    sm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
    md: '0 4px 6px -1px rgba(0, 0, 0, 0.1)',
    lg: '0 10px 15px -3px rgba(0, 0, 0, 0.1)',
    xl: '0 20px 25px -5px rgba(0, 0, 0, 0.1)'
  }
};
```

---

## 🔄 Workflow використання

1. **Відкрийте BlockPicker** (`/builder`) - виберіть потрібний блок
2. **Налаштуйте блок** - змініть текст, кольори, розміри тощо
3. **Перегляньте в Live Preview** - побачте зміни в реальному часі
4. **Додайте responsive налаштування** - для mobile та tablet
5. **Додайте анімації** - вхід, hover ефекти
6. **Експортуйте код** - скопіюйте або завантажте HTML/CSS
7. **Вставте в Elementor** - у HTML віджет

---

## 💡 Поради та рекомендації

### Оптимізація продуктивності
- Використовуйте lazy loading для зображень
- Мінімізуйте CSS та JavaScript
- Використовуйте CSS змінні для глобальних значень

### Доступність
- Завжди додавайте alt текст до зображень
- Використовуйте семантичні HTML теги
- Забезпечте достатній контраст кольорів

### SEO
- Використовуйте правильну ієрархію заголовків (H1-H6)
- Додавайте structured data (Schema.org)
- Оптимізуйте зображення

### Responsive дизайн
- Тестуйте на різних пристроях
- Використовуйте mobile-first підхід
- Приховуйте непотрібні елементи на mobile

---

## 🐛 Troubleshooting

### Блок не відображається
- Перевірте, чи правильно зареєстрований тип блоку
- Перевірте console на помилки
- Переконайтеся, що всі обов'язкові поля заповнені

### Анімації не працюють
- Переконайтеся, що додано CSS для анімацій
- Перевірте trigger анімації (on-load, on-scroll тощо)
- Перевірте з-index елементів

### Responsive налаштування не застосовуються
- Перевірте медіа-запити в CSS
- Переконайтеся, що breakpoints правильно налаштовані
- Перевірте порядок CSS правил (mobile-first)

---

## 📞 Підтримка

Якщо у вас є питання або проблеми:
1. Перевірте цю документацію
2. Подивіться приклади в `src/examples/`
3. Перевірте типи в `src/types/blockTypes.ts`
4. Перегляньте існуючі блоки в `src/lib/blockRegistry.ts`

---

## 🎉 Готово!

Тепер у вас є потужна система з 60+ блоками для створення будь-яких компонентів для вашого WordPress сайту!

**Успіхів у створенні чудових блоків! 🚀**
