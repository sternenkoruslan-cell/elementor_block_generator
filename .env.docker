# Docker Environment Variables (не використовується напряму, див. docker-compose.yml)
# Цей файл тільки для референсу

NODE_ENV=production
PORT=3000
DATABASE_URL=mysql://user:password@mysql:3306/elementor_db
JWT_SECRET=your_jwt_secret_change_in_production
VITE_APP_ID=docker
OAUTH_SERVER_URL=http://localhost:3000/api/oauth
OWNER_OPEN_ID=admin
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
